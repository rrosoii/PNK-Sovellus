import 'package:flutter/widgets.dart';

// Shared route observer used to detect when a page becomes visible again
final RouteObserver<PageRoute<dynamic>> appRouteObserver =
    RouteObserver<PageRoute<dynamic>>();
